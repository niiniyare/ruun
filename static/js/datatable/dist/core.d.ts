import type { DataTableCore, DataTableOptions, DataTableState, DataTableEvents, Row, RowData, RowId, Column, SortState, MultiSortState, SortDirection, Filter, FilterState, PaginationState, SelectionState, EventCallback, Plugin } from './types';
export declare class DataTable<T extends RowData = RowData> implements DataTableCore<T> {
    private options;
    private state;
    private eventEmitter;
    private stateManager;
    private filterEngine;
    private sortEngine;
    private plugins;
    private initialized;
    private destroyed;
    constructor(options: DataTableOptions<T>);
    private normalizeOptions;
    private createInitialState;
    private initialize;
    setData(data: T[]): void;
    getData(): Row<T>[];
    addRow(data: T): void;
    updateRow(id: RowId, data: Partial<T>): void;
    deleteRow(id: RowId): void;
    clearData(): void;
    getColumns(): Column<T>[];
    setColumns(columns: Column<T>[]): void;
    showColumn(columnId: string): void;
    hideColumn(columnId: string): void;
    toggleColumn(columnId: string): void;
    private toggleColumnVisibility;
    sort(columnId: string, direction?: SortDirection): void;
    multiSort(sorts: SortState[]): void;
    clearSort(): void;
    getSortState(): SortState | MultiSortState | undefined;
    filter(filters: Filter[]): void;
    addFilter(filter: Filter): void;
    removeFilter(columnId: string): void;
    clearFilters(): void;
    search(query: string): void;
    getFilterState(): FilterState;
    goToPage(page: number): void;
    nextPage(): void;
    previousPage(): void;
    firstPage(): void;
    lastPage(): void;
    setPageSize(size: number): void;
    getPaginationState(): PaginationState;
    selectRow(id: RowId): void;
    deselectRow(id: RowId): void;
    toggleRowSelection(id: RowId): void;
    selectAll(): void;
    deselectAll(): void;
    getSelectedRows(): Row<T>[];
    getSelectionState(): SelectionState;
    private updateSelectionState;
    getState(): DataTableState<T>;
    setState(newState: Partial<DataTableState<T>>): void;
    resetState(): void;
    private saveState;
    private restoreState;
    reload(): Promise<void>;
    private buildServerParams;
    private recompute;
    on<K extends keyof DataTableEvents<T>>(event: K, callback: EventCallback<Parameters<DataTableEvents<T>[K]>[0]>): () => void;
    off<K extends keyof DataTableEvents<T>>(event: K, callback: EventCallback<Parameters<DataTableEvents<T>[K]>[0]>): void;
    emit<K extends keyof DataTableEvents<T>>(event: K, data?: any): void;
    use(plugin: Plugin<T>): void;
    private getRowId;
    private ensureNotDestroyed;
    destroy(): void;
}
export declare function createDataTable<T extends RowData = RowData>(options: DataTableOptions<T>): DataTable<T>;
//# sourceMappingURL=core.d.ts.map